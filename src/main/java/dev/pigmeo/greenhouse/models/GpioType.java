package dev.pigmeo.greenhouse.models;

/**
 * SWITCH = 0 or 1 / On or Off
 * BUTTON = Changes the state of the pin on and off rapdly, like a button press
 */
public enum GpioType {
    SWITCH, BUTTON
}
